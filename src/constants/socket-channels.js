export const PUSH_TO_REMOTE = (variant) =>`push-to-${variant}`;
export const PUSH_TO_CLIENT = (variant) =>`message-to-${variant}`;

export const EVENT_CONNECTION_INIT = 'event_connection_init';
export const EVENT_DEVICE_CONNECTED = `socket-device-connected`;
export const ACTION_DEVICE_SYNCHING = `socket-device-synching`;
