/* eslint-disable */
export default (user) => {
  const { name, last_name, birthday, phone_number, usc_number } = user
  return {
    name,
    last_name,
    birthday,
    phone_number,
    usc_number,
  }
}
